import { ThemedView } from "@/components/ThemedView";
import { ThemedText } from "@/components/ThemedText";

export default function Credentials() {
    return (
        <ThemedView>
            <ThemedText>Credentials</ThemedText>
        </ThemedView>
    )
}